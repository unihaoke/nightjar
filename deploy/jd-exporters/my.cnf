# jd 项目的 mysql-exporter 连接配置（只读监控账号）。
#
# 由 deploy/jd-exporters/docker-compose.jd.yml 挂载使用。
# 注意：含明文口令，生产请改为环境变量注入或限制文件权限（0600）。
# host 指向 compose 服务名 mysql（同网络内解析）。

[client]
user = exporter
password = exporter_change_me
host = mysql
port = 3306
