# 编译产物与本地运行数据
middleware-ops/middleware-ops
middleware-ops/mwops-agent
middleware-ops/data/
middleware-ops/*.exe
middleware-ops/agent.yaml

# 本地构建输出
.build/

# 前端
middleware-ops-web/node_modules/
middleware-ops-web/dist/
middleware-ops-web/*.log
middleware-ops-web/.vite/
middleware-ops-web/*.tsbuildinfo

# 构建缓存
.gocache/

# 环境与密钥
.env
*.key
*.pem
# Exporter 凭据文件（含明文口令）：仓库只保留 .example 模板
deploy/exporters/my.cnf
!deploy/exporters/my.cnf.example

# 编辑器与系统
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# 临时文件
*.tmp
